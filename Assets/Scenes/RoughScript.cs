using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoughScript : MonoBehaviour
{
    private int[] balloonCols;
    // Start is called before the first frame update
    void Start()
    {
        balloonCols = new int[0];
        balloonCols = 2;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
